<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<!-- 쿼리문을 정의 -->
<!-- namespace : XML내에서 중복된 id를 사용할 수 있도록 구분하는 것. (자바의 패키지와 비슷한 용도.) -->
<mapper namespace="kr.co.sist.exam1">
	<!-- 컬럼 하나에 레코드 하나 조회 할때. -->
	<!-- MyBatis는 자바의 모든 데이터형을 그대로 사용할 수 있다. -->
	<!-- String data = SqlSession.selectOne("singleColumn") -->
	<select id="singleColumn" resultType="String">
		SELECT DNAME
		FROM DEPT
		WHERE
		DEPTNO=10
	</select>

	<!-- 컬럼 하나에 레코드 여러개 조회 할때.
			resultType="자바 데이터형"
			호출 : List<데이터형> List=sqlSession.selectList("id")
			기본형을 사용해도 되고, 기본형에 대응되는 클래스(Wrapper 클래스)를 사용할 수 있다.
	 -->
	 <select id="multiRow" resultType="integer">
	 	SELECT DEPTNO
	 	FROM DEPT
	 </select>
	
	
	<!-- 컬럼 여러개 레코드 하나 조회 할때. -->
	<!-- <typeAlias> : Domain이나 VO를 미리 등록(iBATIS - mapper정의, MyBatis-설정파일)해두고 
		짧은 이름으로 사용할 때. -->
	<!-- 조회되는 컬럼은 대소문자를 구분하지 않지만 setter method는 대소문자를 구분한다. -->
	<!-- iBATIS에서는 컬럼명 또는 컬럼명 as setter명. -->
	<!-- < ... resultType="패키지명.Domain명 또는 typeAlias의 id가 사용된다." -->
	<!-- <select id="multiColumn" resultType="di"> -->
	<select id="multiColumn" resultType="kr.co.sist.exam.domain.DeptInfo">
		SELECT DNAME, LOC
		FROM DEPT
		WHERE DEPTNO=10
	</select>

	<!-- 컬럼 여러개 레코드 여러개 조회 할때. -->
	<!-- < 조회 할때. -->
	<!-- > 조회 할때. -->
	<!-- 동일 쿼리의 처리 -->
	<!-- like 조회 할때. -->
	<!-- subquery 조회 할때. -->
	<!-- union 조회 할때. -->
	<!-- join 조회 할때. -->
	<!-- join과 subquery 사용 조회 할때. -->
	<!-- 컬럼명 또는 테이블명 동적일 때 조회 할때. -->
	<!-- Dynamic Query : if, choose, foreach -->
</mapper>