<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<!-- 쿼리문을 정의 -->
<!-- namespace : XML내에서 중복된 id를 사용할 수 있도록 구분하는 것. (자바의 패키지와 비슷한 용도.) -->
<mapper namespace="kr.co.sist.exam2">
	<!-- 컬럼 하나에 레코드 하나 조회 할때. -->
	<!-- 컬럼 하나에 레코드 여러개 조회 할때. -->
	<!-- 컬럼 여러개 레코드 하나 조회 할때. -->
	<!-- 컬럼 여러개 레코드 여러개 조회 할때. -->
	<!-- < 조회 할때. -->
	<!-- > 조회 할때. -->
	<!-- 동일 쿼리의 처리 -->
	<!-- like 조회 할때. -->
	<!-- subquery 조회 할때. -->
	<!-- union 조회 할때. -->
	<!-- join 조회 할때. -->
	<!-- join과 subquery 사용 조회 할때. -->
	<!-- 컬럼명 또는 테이블명 동적일 때 조회 할때. -->
	<!-- Dynamic Query : if, choose, foreach -->
</mapper>