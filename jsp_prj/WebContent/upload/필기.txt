*파라메터처리
    HTML Form Control에 입력된 값을 서버에서 처리.
    -이름 유일 => request.getParameter("이름");
    -이름 중복=>String[] = request.getParameterValues("이름");
    -파라메터의 이름만 얻기 
        enumeration = request.getParameterNames();
    
*페이지이동
    -요청시 페이지 이동
        -forward
        -브라우저는 페이지의 이동을 알 수 없다.
        -데이터처리페이지(servlet)와 화면구성페이지(JSP)가 분리될 수 있다.        
        1.이동할 페이지를 설정
            RequesDispatcher = request.getRequestDisptcher("URI")        
        2.이동
            rd.forword(request,response);

    -응답시 페이지 이동
        -redirect
        -브라우저는 페이지의 이동을 알게된다. (브라우저가 지정된 페이지를 재요청)
        -정상적인 요청인 경우 보여줄 페이지에서 비정상적인 요청이 발생한 경우에 대한 대비.
        -서버 외부로 보낼 수 있다.(URL)